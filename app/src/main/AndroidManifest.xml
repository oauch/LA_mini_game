<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.la">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LA">
        <activity android:name=".Mackcha"></activity>
        <activity android:name=".Recipe" />
        <activity android:name=".GuessNum" />
        <activity android:name=".Jaum" />
        <activity android:name=".Ibbal" />
        <activity android:name=".Apart" />
        <activity android:name=".Hack" />
        <activity android:name=".CatchSoju" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>